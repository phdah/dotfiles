-- Compiler defaults to latexmk, which is the recommended backend, so no
-- vim.g.vimtex_compiler_method is set here.

-- Use Zathura for PDF preview with SyncTeX forward/inverse search
vim.g.vimtex_view_method = "zathura"

-- Keep the compiler quickfix window closed by default
vim.g.vimtex_quickfix_mode = 0
